# 域名发布地址页

当前仓库现在包含两部分：

- `index.html` / `admin.html`：静态前台与后台演示页面。
- `backend/`：新增的 Python 安全后台，支持远程 PostgreSQL、用户登录、订阅管理、加密内容和审计日志。

如果你要把数据库部署到别的服务器，请优先查看 `backend/README.md` 和 `backend/.env.example`。
